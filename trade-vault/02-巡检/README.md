# 每日巡检

本目录只保存收盘后的巡检报告，文件名使用 `YYYY-MM-DD.md`。

巡检报告由 Agent 在收盘后生成，事实来源是 MySQL、`bin/trade` 和 MCP，不从历史 Markdown 快照推断事实。

报告必须覆盖：

- 当天 pipeline 和系统健康状态
- 交易建议、人工确认、模拟盘订单和风控事件
- 数据源、DB、MCP、CLI 的异常
- 持仓、现金、候选池的重要变化
- 明天开盘前需要人工处理的事项
